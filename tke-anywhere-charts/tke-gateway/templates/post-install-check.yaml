apiVersion: batch/v1
kind: Job
metadata:
  name: tke-gateway-post-install-check
  namespace: {{ .Release.Namespace | quote }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "3"
    "helm.sh/hook-delete-policy": hook-succeeded, before-hook-creation
  labels:
    app: tke-gateway-post-install-check
spec:
  template:
    metadata:
      labels:
        app: tke-gateway-post-install-check
    spec:
      tolerations:
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: "NoSchedule"
      containers:
        - name: kubectl
          image: "{{ .Values.registry }}/{{ .Values.kubectl.image.repository }}:{{ .Values.kubectl.image.tag }}"
          imagePullPolicy: "{{ .Values.kubectl.image.imagePullPolicy }}"
          command: ['/app/hook/chart-hook-check.sh']
          args:
            - gateway-post-install-check
          volumeMounts:
            - name: chart-hook-check
              mountPath: /app/hook
      volumes:
        - name: chart-hook-check
          configMap:
            name: tke-gateway-chart-hook-check
            defaultMode: 0774
      restartPolicy: OnFailure
